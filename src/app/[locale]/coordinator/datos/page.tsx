'use client'

export const dynamic = 'force-dynamic'

import DatosPage from '@/components/datos/DatosPage'

export default function CoordinatorDatosPage() {
  return <DatosPage showSidebar={false} />
}
